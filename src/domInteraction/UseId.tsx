// Generates a unique ID for accessibility

// Unique on each render

// Helps for mixing SSR and CSR
export default function UseId() {


    return (
        <div>
            UseId
        </div>
    )
}